spring:
  h2:
    console:
      # Console H2 habilitado no profile cli para inspeção manual do banco
      enabled: true
      path: /h2-console
  shell:
    interactive:
      enabled: true

server:
  address: 127.0.0.1
